  
{
  "version": 2,
  "readOnly": false,
  "clients": [
    {
      "name": "ml-local",
      "autoaddrlist": false,
      "addrlist": "pvxs-lab-ml pvxs-lab-ml-ioc",
      "tls_keychain": "/home/gateway/.config/pva/1.5/gateway.p12"
    }
  ],
  "servers": [
    {
      "name": "gateway",
      "clients": ["ml-local"],
      "acf_client": "ml-local",
      "statusprefix": "GW:STS:",
      "//": "The DMZ address, substituted at start: podman assigns it, and binding every",
      "//interface": "interface would answer department workstations twice for one name.",
      "interface": ["__SERVE_ADDR__"],
      "autoaddrlist": false,
      "addrlist": "pvxs-lab-ml",
      "tls_keychain": "/home/gateway/.config/pva/1.5/gateway.p12",
      "serverport": "5175",
      "//peer": "Its server side has to ask the lab department whether a certificate it is",
      "//peer2": "presented still stands, and only that department can answer. The peer",
      "//peer3": "gateway is named, never the peer certificate manager: the two gateways",
      "//peer4": "share the perimeter segment, and this is the only thing that crosses it.",
      "//peer5": "Read by the inner client alone, so nothing else here is given the route.",
      "EPICS_PVAS_STATUS_NAME_SERVERS": "pvxs-lab-gateway:5075",
      "EPICS_PVAS_TLS_PORT": "5176",
      "bcastport": "5176",
      "access": "gateway.acf",
      "pvlist": "gateway.pvlist"
    },
    {
      "name": "downstream_status",
      "clients": [],
      "interface": ["127.0.0.1"],
      "serverport": "0",
      "statusprefix": "GW:STS:"
    }
  ]
}
