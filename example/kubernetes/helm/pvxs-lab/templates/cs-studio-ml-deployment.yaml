apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "pvxs-lab.csStudioMlService" . }}
  labels:
    {{- include "pvxs-lab.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.csStudioMl.replicas }}
  selector:
    matchLabels:
      app: cs-studio-ml
      release: {{ .Release.Name }}
  template:
    metadata:
      labels:
        app: cs-studio-ml
        release: {{ .Release.Name }}
    spec:
      containers:
        - name: cs-studio-ml
          image: {{ include "pvxs-lab.image" (dict "Values" .Values "name" .Values.images.csStudio.name "tag" .Values.images.csStudio.tag) | quote }}
          imagePullPolicy: {{ .Values.imagePullPolicy }}
          command: ["/bin/bash", "-lc"]
          args:
            - |
              set -euo pipefail
              # Fix: UDP PVA search to ClusterIP services doesn't work in k8s.
              # Move PVACMS service from ADDR_LIST (UDP) to NAME_SERVERS (TCP).
              sed -i 's|EPICS_PVA_ADDR_LIST="pvxs-lab-ml |EPICS_PVA_ADDR_LIST="|' /home/mloperator/.mloperator_bashrc
              sed -i 's|EPICS_PVA_NAME_SERVERS="|EPICS_PVA_NAME_SERVERS="{{ include "pvxs-lab.mlService" . }}:5075 |' /home/mloperator/.mloperator_bashrc
              exec /usr/bin/supervisord -c /etc/supervisor/supervisord.conf
          volumeMounts:
            - name: pva-config
              mountPath: /home/mloperator/.config/pva/1.5
              subPath: mloperator
            - name: pva-config
              mountPath: /home/mlsystem/.config/pva/1.5
              subPath: mlsystem
            - name: krb5-conf
              mountPath: /etc/krb5.conf
              subPath: krb5.conf
              readOnly: true
          env:
            - name: EPICS_AUTH_KRB_REALM
              value: "EPICS.ORG"
          ports:
            - name: novnc
              containerPort: 8080
      volumes:
        - name: pva-config
          {{- if .Values.csStudioMl.persistence.enabled }}
          persistentVolumeClaim:
            claimName: {{ include "pvxs-lab.fullname" . }}-cs-studio-ml-pva
          {{- else }}
          emptyDir: {}
          {{- end }}
        - name: krb5-conf
          configMap:
            name: {{ include "pvxs-lab.fullname" . }}-krb5
