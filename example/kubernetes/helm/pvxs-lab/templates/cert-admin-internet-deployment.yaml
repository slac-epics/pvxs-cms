apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "pvxs-lab.certAdminInternetService" . }}
  labels:
    {{- include "pvxs-lab.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.certAdminInternet.replicas }}
  selector:
    matchLabels:
      app: cert-admin-internet
      release: {{ .Release.Name }}
  template:
    metadata:
      labels:
        app: cert-admin-internet
        release: {{ .Release.Name }}
    spec:
      containers:
        - name: cert-admin-internet
          image: {{ include "pvxs-lab.image" (dict "Values" .Values "name" .Values.images.csStudio.name "tag" .Values.images.csStudio.tag) | quote }}
          imagePullPolicy: {{ .Values.imagePullPolicy }}
          command: ["/bin/bash", "-lc"]
          args:
            - exec /usr/bin/supervisord -c /etc/supervisor/supervisord.conf
          volumeMounts:
            {{- include "pvxs-lab.issuersMount" (dict "ctx" .) | nindent 12 }}
            - name: pva-config
              mountPath: /home/operator/.config/pva/1.5
              subPath: operator
            - name: pva-config
              mountPath: /home/guest/.config/pva/1.5
              subPath: guest
            - name: krb5-conf
              mountPath: /etc/krb5.conf
              subPath: krb5.conf
              readOnly: true
          env:
          {{- /* Both ids, and no pod-level EPICS_PVA_AUTH_ISSUER: the user's profile selects. */}}
          {{- include "pvxs-lab.issuersEnv" (dict "ctx" .) | nindent 12 }}
            - name: EPICS_AUTH_KRB_REALM
              value: "EPICS.ORG"
          ports:
            - name: novnc
              containerPort: 8080
      volumes:
        {{- include "pvxs-lab.issuersVolume" (dict "ctx" .) | nindent 8 }}
        - name: pva-config
          {{- if .Values.certAdminInternet.persistence.enabled }}
          persistentVolumeClaim:
            claimName: {{ include "pvxs-lab.fullname" . }}-cert-admin-internet-pva
          {{- else }}
          emptyDir: {}
          {{- end }}
        - name: krb5-conf
          configMap:
            name: {{ include "pvxs-lab.fullname" . }}-krb5
