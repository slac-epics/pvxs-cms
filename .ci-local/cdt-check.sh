#!/bin/sh
set -e

# CDT check disabled - global constructors are used intentionally for plugin auto-registration
echo "CDT check disabled - global constructors used for plugin auto-registration"
exit 0
